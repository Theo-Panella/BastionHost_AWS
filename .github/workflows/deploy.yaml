name: "Deploy for AWS - Testing level"
on:
  push:
    branches:
      - dev

jobs:
  Configuration:
    runs-on: ubuntu-latest

    steps:
      - name: "Checkout do código"
        uses: actions/checkout@v3
      - name: "Copiando Código"

